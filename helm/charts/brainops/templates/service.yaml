apiVersion: v1
kind: Service
metadata:
  name: {{ include "brainops.fullname" . }}
  labels:
    {{- include "brainops.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.healthPort }}
      targetPort: health
      protocol: TCP
      name: health
  selector:
    {{- include "brainops.selectorLabels" . | nindent 4 }}
